# Option 1 (recommended for local dev): keep empty and use Vite proxy (/api -> http://localhost:8080)
# VITE_API_BASE_URL=

# Option 2: set absolute backend URL in production
# VITE_API_BASE_URL=http://localhost:8080
